## 1. แก้ไขโครงสร้างเมนูกลุ่มการตลาด

- [x] 1.1 ใน `src/components/AppSidebar.tsx` กลุ่ม `marketing` ลบ item "คอนเทนต์โซเชียล" (`href: ''` + `children`) ออก
- [x] 1.2 เพิ่ม item "คอนเทนต์โซเชียล" (`href: '/content'`, icon `PenTool`, `menuKey: 'marketing'`) ไว้หลัง "แคมเปญอีเมล"
- [x] 1.3 เพิ่ม item "แดชบอร์ดคอนเทนต์" (`href: '/content-dashboard'`, icon `LayoutDashboard`, `menuKey: 'marketing'`) ตามหลัง "ผลงานคอนเทนต์"
- [x] 1.4 เพิ่ม item "ปฏิทินคอนเทนต์" (`href: '/content-planner'`, icon `CalendarDays`, `menuKey: 'marketing'`) ตามหลัง "แดชบอร์ดคอนเทนต์"
- [x] 1.5 ตรวจสอบว่า "วิเคราะห์แคมเปญ" และ "สตูดิโอสื่อ" ยังอยู่ตามหลังรายการคอนเทนต์ (ไม่เปลี่ยน)

## 2. ตรวจสอบความสอดคล้อง Design System

- [x] 2.1 ยืนยันว่าไอคอนทั้งหมด (`PenTool`, `LayoutDashboard`, `CalendarDays`) ถูกนำเข้าอยู่แล้วใน import block (ไม่ต้องเพิ่ม import ใหม่)
- [x] 2.2 ยืนยันว่า `menuKey` ของแต่ละรายการไม่เปลี่ยน (`marketing` สำหรับรายการคอนเทนต์, `media_studio` สำหรับสตูดิโอสื่อ)
- [x] 2.3 ยืนยันว่าไม่มีการสร้าง component/pattern ใหม่ — ใช้เฉพาะโครงสร้าง `NavItem` แบบแบนราบเดิม

## 3. ตรวจสอบและเทสต์

- [x] 3.1 รัน `pnpm lint` เพื่อตรวจสอบ TypeScript/ESLint
- [x] 3.2 รัน `pnpm build` เพื่อยืนยัน production build ผ่าน
- [x] 3.3 ตรวจสอบ UI บนเบราว์เซอร์: กลุ่ม "การตลาด" แสดง 6 รายการแบบแบนราบ (ไม่มี sub-group "คอนเทนต์โซเชียล") และทุก link นำทางถูกต้อง

## 4. ปรับลำดับ: แดชบอร์ดคอนเทนต์อยู่บนสุด

- [x] 4.1 ใน `src/components/AppSidebar.tsx` กลุ่ม `marketing` ย้าย item "แดชบอร์ดคอนเทนต์" (`/content-dashboard`) ขึ้นเป็นรายการแรกสุด (ก่อน "แคมเปญอีเมล")
- [x] 4.2 ตรวจสอบลำดับสุดท้ายเป็น: แดชบอร์ดคอนเทนต์ → แคมเปญอีเมล → คอนเทนต์โซเชียล → ปฏิทินคอนเทนต์ → วิเคราะห์แคมเปญ → สตูดิโอสื่อ
- [x] 4.3 รัน `pnpm lint` และ `pnpm build` เพื่อยืนยันผ่าน
- [x] 4.4 ตรวจสอบ UI บนเบราว์เซอร์: "แดชบอร์ดคอนเทนต์" อยู่บนสุดของกลุ่มการตลาด และทุก link นำทางถูกต้อง
- [x] 4.5 ใน `src/components/AppSidebar.tsx` เปลี่ยน title ของ item `/content` จาก "ผลงานคอนเทนต์" เป็น "คอนเทนต์โซเชียล" (คง href และ icon เดิม)
