## 1. แก้บั๊ก setContent

- [x] 1.1 แก้ `ArticleEditor.tsx` บรรทัด 147 (`useEffect` sync เมื่อ `html` prop เปลี่ยนจากภายนอก): เปลี่ยน `editor.commands.setContent(html, false)` เป็น `editor.commands.setContent(html, { emitUpdate: false })`
- [x] 1.2 แก้ `ArticleEditor.tsx` บรรทัด 206 (`exitSourceMode`): เปลี่ยน `editor.commands.setContent(sourceHtml, false)` เป็น `editor.commands.setContent(sourceHtml, { emitUpdate: false })`
- [x] 1.3 **เพิ่มระหว่าง implement (พบตอนทดสอบ 2.3):** แก้ `enterSourceMode()` เปลี่ยน `setSourceHtml(editor.getHTML())` เป็น `setSourceHtml(html)` (ใช้ prop ต้นทางแทน HTML ที่ผ่าน schema ของ Tiptap แล้ว ซึ่งตัดสไตล์ที่ไม่รู้จักทิ้งไปก่อนหน้านี้)
- [x] 1.4 **เพิ่มระหว่าง implement (พบตอนทดสอบสลับแท็บ "แก้ไข"/"ตัวอย่าง" ไปมาโดยผู้ใช้รายงาน):** เพิ่มเงื่อนไข `if (!editor.isFocused) return;` ที่ต้นฟังก์ชัน `onUpdate` ใน `ArticleEditor.tsx` — ป้องกัน transaction ที่ตั้งค่า `content` เริ่มต้นตอนสร้าง/remount editor (ซึ่งเกิดทุกครั้งที่ Radix Tabs unmount/mount แท็บ, เกิดก่อนผู้ใช้คลิกเข้าไปในกล่องข้อความ) ไม่ให้ยิง `onChange` ทับ `campaignBody` ด้วย HTML ที่ผ่าน schema (ตัดสไตล์) ทั้งที่ยังไม่มีใครแก้ไขอะไรจริง (ลองวิธี `skipNextUpdateRef` แบบไม่มีเงื่อนไขมาก่อน แล้วพบว่าพังกรณี editor เริ่มจากค่าว่างเปล่า — เปลี่ยนมาใช้ `isFocused` แทน ดูรายละเอียดใน design.md decision 3)

## 2. ตรวจสอบ

- [x] 2.1 `pnpm lint` และ `pnpm build` ผ่าน ไม่มี error ใหม่ (lint: 0 errors, 47 warnings เดิม; build: สำเร็จใน 14.30s / 15.09s หลังแก้ 1.4)
- [x] 2.6 **เพิ่มระหว่าง implement:** ทดสอบสลับแท็บ "แก้ไข" ↔ "ตัวอย่าง" ไปมาซ้ำๆ (4 รอบ) โดยไม่แตะ source mode และไม่พิมพ์แก้ไขอะไรเลย → เช็คว่าสีพื้นหลัง/ปุ่ม CTA ในแท็บ "ตัวอย่าง" ยังอยู่ครบทุกรอบ ไม่หายไปเอง — ผ่านหลังเพิ่ม 1.4 (ก่อนแก้ 1.4 เจอปัญหานี้เป็นระยะ ไม่ทุกครั้ง เพราะขึ้นกับว่า `requestAnimationFrame` ที่ deferred ไว้มีเวลาได้รันจริงก่อนเช็คหรือไม่)
- [x] 2.7 **เพิ่มระหว่าง implement (regression ที่พบจากการแก้ 1.4 รอบแรกด้วย `skipNextUpdateRef` แบบไม่มีเงื่อนไข ก่อนเปลี่ยนมาใช้ `isFocused`):** เปิดหน้า "สร้างแคมเปญ" แบบไม่เลือก template (เนื้อหาว่างเปล่า) → คลิกเข้ากล่องข้อความแล้วพิมพ์ทันที → เช็คแท็บ "ตัวอย่าง" ว่าตัวอักษรที่พิมพ์ไปไม่หายไป — ผ่านหลังแก้เป็น `isFocused` (ตอนใช้ `skipNextUpdateRef` แบบเดิมพบว่าตัวอักษรแรกหายไปจริง เป็น regression ที่แก้แล้ว)
- [x] 2.2 ทดสอบจริงในเบราว์เซอร์: เปิดหน้า "สร้างแคมเปญ" → เลือก template ที่มีพื้นหลังสี (เช่น Elegant Purple) → ไม่พิมพ์แก้ไขอะไร → เปิดแท็บ "ตัวอย่าง" → ยืนยันว่าสีพื้นหลังตรงกับที่เห็นในแท็บ Template (ไม่ใช่พื้นขาว) — ผ่าน เห็นแถบสีม่วง gradient และปุ่ม CTA "Visit Our Website" ครบถ้วน
- [x] 2.3 ทดสอบออกจาก source mode: เปิด template ใดก็ได้ → กดปุ่มดู HTML ดิบ (`</>`) → ออกจาก source mode โดยไม่แก้ไขอะไร → เช็คแท็บ "ตัวอย่าง" ว่าสไตล์ยังอยู่ครบ ไม่หาย — ผ่าน (พบปัญหาก่อนแก้ 1.3 แล้วแก้เพิ่มแล้วทดสอบซ้ำผ่าน) source mode แสดง `<!DOCTYPE html>` เต็มพร้อม `background:linear-gradient` ครบ, preview หลังออกจาก source mode ยังเห็นแถบสีม่วงและปุ่ม "Visit Our Website" ถูกต้อง
- [x] 2.4 ทดสอบ regression: พิมพ์แก้ไขข้อความในแท็บ "แก้ไข" จริงๆ (เช่น เพิ่มประโยค) → ยืนยันว่าการแก้ไขยังถูกบันทึกเข้า `campaignBody` ตามปกติ (การแก้บั๊กนี้ต้องไม่ทำให้การแก้ไขจริงของผู้ใช้หยุดทำงาน) — ผ่าน พิมพ์ข้อความเพิ่มแล้วเห็นตรงกันทั้งแท็บ "แก้ไข" และ "ตัวอย่าง" (หมายเหตุ: พื้นหลังสี header หายไปหลังพิมพ์แก้ไขจริง เพราะเนื้อหาต้อง round-trip ผ่าน schema ของ Tiptap ตามธรรมชาติของการแก้ไขจริง — ตรงกับ Non-Goal ที่ระบุไว้ใน design.md ไม่ใช่ regression ใหม่)
- [x] 2.5 ทดสอบหน้าที่ใช้ `ArticleEditor` ร่วมกัน (เขียนบทความคอนเทนต์) ว่ายังทำงานปกติ ไม่มี regression จากการแก้ 2 จุดนี้ — ผ่าน เปิดบทความจริง ("ก้าวล้ำไปอีกขั้น KTNBS DocCapture...") เข้า/ออก source mode แล้วเนื้อหา + ลิงก์ `<a href>` ในบทความยังครบถ้วน ไม่หาย ปิดโดยไม่บันทึก ไม่กระทบข้อมูลจริง
