## 1. ปรับ widget "แพลตฟอร์ม" ให้แสดง logo + ชื่อ + จำนวน

- [x] 1.1 ใน `src/pages/ContentDashboardPage.tsx` เปลี่ยนการแสดงชื่อแพลตฟอร์มจาก Badge เป็น Logo Icon (`PlatformIcon`) + ชื่อ ในรูปแบบเดียวกับ "สถานะช่องทาง" (กล่อง `w-8 h-8 rounded-lg` ตาม `getPlatformColors`)
- [x] 1.2 เอา Badge ที่แสดงชื่อแพลตฟอร์มซ้ำออก (แสดงชื่อเพียงครั้งเดียว)

## 2. เรียงลำดับแพลตฟอร์ม

- [x] 2.1 เปลี่ยน sort เป็น `เรียงตามจำนวนคอนเทนต์มาก→น้อย และชื่อแพลตฟอร์ม A–Z เมื่อจำนวนเท่ากัน` (`(b[1]-a[1]) || nameA.localeCompare(nameB)`)
- [x] 2.2 ยืนยันจำนวนคอนเทนต์แสดงตามข้อมูลจริง (`platformCounts`)

## 3. ตรวจสอบและทดสอบ

- [x] 3.1 รัน `pnpm lint` และ `pnpm build` ให้ผ่าน
- [ ] 3.2 ทดสอบบนเบราว์เซอร์: เปิดแดชบอร์ดคอนเทนต์ — widget "แพลตฟอร์ม" แสดง logo + ชื่อ (ครั้งเดียว) + จำนวน เรียงตามจำนวนมาก→น้อย และชื่อ A–Z เมื่อเท่ากัน
