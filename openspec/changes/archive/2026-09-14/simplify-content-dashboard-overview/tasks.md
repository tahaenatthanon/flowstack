## 1. การ์ด "ความคืบหน้าการผลิต" เป็นแหล่งข้อมูลเดียว

- [x] 1.1 ใน `ContentDashboardPage.tsx` เพิ่ม `'rejected'` เข้า `workProgressStatuses` ต่อจาก `'draft'` (ลำดับ: published, approved, pending_approval, revision, draft, rejected) — ใช้สี/ไอคอนจาก `STATUS_MAP.rejected` ที่มีอยู่แล้ว ไม่ต้องเพิ่ม token สีใหม่
- [x] 1.2 ลบ `productionStatCards` array และ grid 4 ใบที่ render มันออกทั้งหมด (บรรทัดของ stat card เดิมบนสุดของแท็บภาพรวม)
- [x] 1.3 ตรวจสอบว่า footer "เนื้อหาทั้งหมด" ของการ์ดความคืบหน้าการผลิต (ที่มีอยู่แล้ว) ยังคงแสดงยอดรวมถูกต้องหลังเพิ่มสถานะที่ 6

## 2. แยก "สุขภาพคิวเผยแพร่" เป็นการ์ด "เผยแพร่ล้มเหลว" + "คิวเผยแพร่"

- [x] 2.1 สร้างการ์ด "เผยแพร่ล้มเหลว" ใหม่ (ย้าย list `queue.failures` + ปุ่ม "ลองส่งใหม่" + logic `handleRetry` เดิมมาไว้) วางในคอลัมน์ซ้าย ("ต้องดำเนินการ")
- [x] 2.2 เพิ่ม `Badge` แสดงจำนวน `queue.failures.length` ใน `CardTitle` ของการ์ด "เผยแพร่ล้มเหลว" (ใช้ pattern เดียวกับ `ImpactOSPage.tsx` บรรทัด 1379-1380) — ไม่แสดง badge เมื่อจำนวนเป็น 0
- [x] 2.3 แก้การ์ด "สถานะการเผยแพร่" เดิมให้เหลือแค่ตัวนับ `pending`/`processing`/`sent` เปลี่ยนชื่อเป็น "คิวเผยแพร่" วางในคอลัมน์ขวา ("ภาพรวม") — ตัดกริดที่เคยมี `failed` และปุ่มลองส่งใหม่ออก
- [x] 2.4 ลบข้อความแจ้งเตือน "เลยกำหนด" (`queue.overdue_pending`) ที่เคย inline อยู่ในการ์ดคิวเผยแพร่ออก — เหลือ banner บนสุดของหน้า (`useOverdueCount()`) เป็นจุดแจ้งเตือนเดียว

## 3. การ์ด "คอนเทนต์ค้างท่อ" เพิ่ม Badge

- [x] 3.1 เพิ่ม `Badge` แสดง `aging.total` ใน `CardTitle` ของการ์ด "คอนเทนต์ค้างท่อ" (เดิมชื่อ "คอนเทนต์ที่ยังไม่เผยแพร่" ในโค้ด) — ไม่แสดง badge เมื่อจำนวนเป็น 0
- [x] 3.2 ยืนยันกับผู้ใช้ก่อน/ระหว่าง apply ว่า badge ควรนับรวมทั้งหมด (`aging.total`) ตามที่ระบุใน design.md หรือเฉพาะกลุ่มค้างเกิน 31 วัน (`d31_90 + d90_plus`) แล้วปรับตามคำตอบ — ยืนยันแล้ว: รวมทั้งหมด (`aging.total`)

## 4. ตัด Funnel และ สถานะสร้างสื่อ AI

- [x] 4.1 ลบการ์ด "Funnel การผลิต" ทั้งหมด (JSX + `funnelStages` derived state) ออกจากแท็บภาพรวม — ไม่แก้ backend
- [x] 4.2 ลบการ์ด "สถานะสร้างสื่อ AI" ทั้งหมด (JSX + `assetKinds`/`assetStatuses`/`hasAssetActivity` derived state) ออกจากแท็บภาพรวม — ไม่แก้ backend

## 5. ตัดการ์ด "คิวรออนุมัติ" ออกจากภาพรวม

- [x] 5.1 ลบการ์ด "รออนุมัติ" (pending queue list) ทั้งหมดออกจากคอลัมน์ขวาเดิม รวมถึง derived state `pendingItems` ถ้าไม่ถูกใช้ที่อื่นในไฟล์แล้ว

## 6. ย้าย "สถานะช่องทาง" ไปแท็บวิเคราะห์

- [x] 6.1 ย้าย JSX การ์ด "สถานะช่องทาง" (channels + `channelStatus` + `getPlatformColors`) จาก `ContentDashboardPage.tsx` ไปเป็นส่วนหนึ่งของ `AnalyticsSocialTab.tsx`
- [x] 6.2 ส่ง props ที่จำเป็น (`channels`, `channelStatus` หรือ query hooks `usePublishChannels`/`useChannelConnectionStatus`) เข้า `AnalyticsSocialTab` — เรียก hook ตรงใน component นั้นได้เลยถ้าเรียบง่ายกว่าการส่งลงมา — เรียก hook ตรงในคอมโพเนนต์นั้นเลย
- [x] 6.3 คงปุ่ม "จัดการ" ที่นำทางไป `/content?tab=settings` ไว้ในตำแหน่งใหม่

## 7. โครงคอลัมน์และหัวข้อ

- [x] 7.1 จัดกลุ่มการ์ดในแท็บภาพรวมใหม่: คอลัมน์ซ้าย (`xl:col-span-2`) = เผยแพร่ล้มเหลว, คอนเทนต์ค้างท่อ; คอลัมน์ขวา (`xl:col-span-1`) = ความคืบหน้าการผลิต, คิวเผยแพร่, กำหนดการโพสต์ถัดไป, เนื้อหาล่าสุด
- [x] 7.2 เพิ่ม `<h3 className="text-sm font-semibold mb-3">ต้องดำเนินการ</h3>` เหนือการ์ดใบแรกของคอลัมน์ซ้าย และ `<h3 className="text-sm font-semibold mb-3">ภาพรวม</h3>` เหนือการ์ดใบแรกของคอลัมน์ขวา (สไตล์เดียวกับ `BudgetPage.tsx` บรรทัด 489)
- [x] 7.3 เมื่อการ์ด "เผยแพร่ล้มเหลว" และ "คอนเทนต์ค้างท่อ" ไม่มีรายการทั้งคู่ ให้แสดงข้อความว่างรวมจุดเดียวแทนการ์ดว่างแยก 2 ใบ (สไตล์ `py-8 text-center text-sm text-muted-foreground`)

## 8. ตรวจสอบ

- [x] 8.1 รัน `pnpm lint` ให้ผ่าน — 0 errors (47 warning เดิมที่ไม่เกี่ยวกับไฟล์นี้)
- [x] 8.2 รัน `pnpm build` ให้ผ่าน — build สำเร็จ ไม่มี TypeScript error
- [x] 8.3 รัน `pnpm test` ให้ผ่าน — 38 ไฟล์/254 เทสต์ผ่านทั้งหมด รวม `ContentDashboardPagePlatforms.test.tsx` ที่ครอบคลุมไฟล์นี้โดยตรง
- [x] 8.4 เปิดหน้า `/content-dashboard` ด้วยสายตา (ผ่าน preview หลังผู้ใช้ login ให้) ตรวจแล้วครบ: 6 แถบสถานะรวม "ปฏิเสธ" (1 ชิ้น, 2%) ผลรวม 100%/50 ชิ้นถูกต้อง, ไม่มี stat card 4 ใบบนสุด, ไม่มี Funnel/AI-asset/คิวรออนุมัติ, badge สีแดง "2" บนการ์ดเผยแพร่ล้มเหลว + badge outline "38" บนการ์ดค้างท่อ (ตรงกับผลรวม bucket 13+7+0+18), การ์ดคิวเผยแพร่เหลือ 3 ตัวนับไม่มี "เลยกำหนด" ซ้ำ, สถานะแพลตฟอร์มย้ายไปแท็บวิเคราะห์ > โซเชียล แล้ว แสดงผลถูกต้องพร้อมป้าย "ไม่ผูกช่วงวันที่ที่เลือก" — ไม่มี console error
- [x] 8.5 ทดสอบ responsive ต่ำกว่า `xl` (800px) ว่าทุก section stack เป็นคอลัมน์เดียวไม่ overflow — ตรวจแล้วถูกต้อง, ทดสอบ desktop (1440px) เห็นโครง 2 คอลัมน์ตามสัดส่วน 2:1 พร้อมหัวข้อ "ต้องดำเนินการ"/"ภาพรวม" ถูกต้อง
