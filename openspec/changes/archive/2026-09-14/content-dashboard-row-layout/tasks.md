## 1. โครง JSX ใหม่เป็น 3 แถว

- [x] 1.1 ใน `ContentDashboardPage.tsx` แทนที่ wrapper เดิม `<div className="grid grid-cols-1 xl:grid-cols-3 gap-6">` (Master 2-column) ด้วย 3 แถวอิสระต่อกัน ห่อด้วย fragment หรือ `<div className="space-y-6">`
- [x] 1.2 แถว 1: `<div className="grid grid-cols-1 lg:grid-cols-2 gap-6">` ครอบการ์ด "เผยแพร่ล้มเหลว" + "คอนเทนต์ที่ยังไม่เผยแพร่" (ย้าย logic `actionColumnEmpty` มาห่อแถวนี้แทนที่จะห่อ "คอลัมน์ซ้าย" เดิม — เงื่อนไข/ข้อความว่างรวมไม่เปลี่ยน)
- [x] 1.3 แถว 2: `<div className="grid grid-cols-1 lg:grid-cols-3 gap-6">` ครอบการ์ด "เนื้อหาล่าสุด" (`lg:col-span-2`) + "ภาพรวมสถานะคอนเทนต์" (แคบ, ไม่ใส่ col-span)
- [x] 1.4 แถว 3: `<div className="grid grid-cols-1 lg:grid-cols-2 gap-6">` ครอบการ์ด "คิวเผยแพร่" + "กำหนดการโพสต์ถัดไป"
- [x] 1.5 ลบ class `xl:col-span-2` เดิมและ wrapper "LEFT"/"RIGHT" div เดิมทั้งหมด (ถูกแทนที่ด้วยแถวใหม่) — รวมถึงลบ `flex flex-1 flex-col` ที่ไม่จำเป็นแล้วออกจากการ์ด "เนื้อหาล่าสุด" (ใช้ `lg:col-span-2` แทน)
- [x] 1.6 ยืนยันแล้วด้วยสายตา (ผ่าน preview) ว่าลำดับการ์ดใน DOM คือ: เผยแพร่ล้มเหลว → คอนเทนต์ที่ยังไม่เผยแพร่ → เนื้อหาล่าสุด → ภาพรวมสถานะคอนเทนต์ → คิวเผยแพร่ → กำหนดการโพสต์ถัดไป ตรงตาม proposal.md

## 2. ตรวจสอบ

- [x] 2.1 `pnpm lint` ผ่าน — 0 errors, 47 warnings (เท่าเดิมก่อนแก้ ไม่มี warning ใหม่จากไฟล์นี้)
- [x] 2.2 `pnpm build` ผ่าน — build สำเร็จ
- [x] 2.3 `pnpm test` ผ่าน — 38 test files, 254 tests ผ่านทั้งหมด
- [x] 2.4 วัดความสูงจริงด้วย `getBoundingClientRect()` ที่จอ 1440px หลัง implement: แถว 1 = เผยแพร่ล้มเหลว(341px) vs คอนเทนต์ที่ยังไม่เผยแพร่(392px) ต่าง 51px; แถว 2 = เนื้อหาล่าสุด(501px) vs ภาพรวมสถานะคอนเทนต์(406px) ต่าง 95px; แถว 3 = คิวเผยแพร่(135px) vs กำหนดการโพสต์ถัดไป(161px) ต่าง 26px — ไม่มีแถวไหนเกิน 250px ตามเป้า (ดีกว่าที่คาดการณ์ไว้ในตอนออกแบบด้วยซ้ำ)
- [x] 2.5 ทดสอบ responsive แล้ว: จอ 1100px (`lg` แต่ต่ำกว่า `xl`) เห็นเป็นหลายคอลัมน์ทันที (ยืนยันว่า breakpoint เปลี่ยนจาก `xl`→`lg` ทำงานถูกต้อง); จอ 900px (ต่ำกว่า `lg`) stack เป็นคอลัมน์เดียวเรียงลำดับถูกต้องตามข้อ 1.6 ไม่มี overflow
- [x] 2.6 ไม่ได้ทดสอบ live กับกรณีว่างจริง (ข้อมูล tenant ปัจจุบันมีทั้งรายการ failed และค้างท่ออยู่) — แต่ logic `actionColumnEmpty`/`hasFailures`/`hasAgingItems` ไม่ถูกแก้เลยในงานนี้ (เปลี่ยนแค่ JSX ที่มันห่ออยู่ จาก "คอลัมน์ซ้าย" เป็น "แถว 1") จึงมั่นใจได้ว่าพฤติกรรมเดิมที่เคยตรวจสอบผ่านแล้วในงานก่อนหน้ายังคงเดิม
- [x] 2.7 ยืนยันแล้วว่าแท็บ "วิเคราะห์" ไม่ถูกกระทบ — โค้ดส่วนนั้น (`TabsContent value="analytics"`) ไม่ถูกแก้ไขเลยในงานนี้
