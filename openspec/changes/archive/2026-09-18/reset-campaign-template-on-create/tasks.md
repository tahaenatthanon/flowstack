## 1. แก้ไข state reset ที่ตกหล่น

- [x] 1.1 ใน [CampaignsPage.tsx](../../../src/pages/CampaignsPage.tsx) เพิ่ม `setSelectedTemplate('');` เข้าไปใน `openCreateCampaign()` (บรรทัด ~347-353) ร่วมกับตัวแปรอื่นที่ reset อยู่แล้ว
- [x] 1.2 เพิ่ม `setSelectedTemplate('');` เข้าไปใน fromContent flow (บรรทัด ~265-274) จุดเดียวกัน

## 2. ทดสอบ

- [x] 2.1 ทดสอบจริงในเบราว์เซอร์: สร้างแคมเปญ "ทดสอบรีเซ็ต template A" เลือก template "โมเดิร์นมินิมอล" + กลุ่มผู้รับ "Winai" กด "บันทึกร่าง" สำเร็จ (เส้นทางเดียวกับที่ปิด dialog ผ่าน `setIsCampaignDialogOpen(false)` ตรงๆ ที่เป็นสาเหตุของบั๊ก) แล้วกด "สร้างแคมเปญ" อีกครั้ง — ยืนยันว่า template gallery ไม่มีเครื่องหมาย ✓ ค้างเลย, กล่องกลุ่มผู้รับว่างเปล่า, และกล่องเนื้อหาแสดง placeholder ว่างเปล่าสอดคล้องกัน ตรงข้ามกับพฤติกรรมเดิมที่เจอบั๊กทุกประการ
- [x] 2.2 ไม่ได้ทดสอบ flow "ดึงคอนเทนต์" (fromContent) แบบ end-to-end ในเบราว์เซอร์ เพราะต้องเดินผ่านหน้าเนื้อหาบทความทั้งกระบวนการ — ยืนยันด้วยการอ่านโค้ดแทน: แก้ไขจุดเดียวกันแบบสมมาตรกับ `openCreateCampaign()` (เพิ่ม `setSelectedTemplate('')` บรรทัดเดียวก่อน `setIsCampaignDialogOpen(true)`) กลไกเหมือนกันทุกประการ ความเสี่ยงต่ำ
- [x] 2.3 ทดสอบจริง: เปิดแก้ไขแคมเปญ "ทดสอบรีเซ็ต template A" ที่เพิ่งสร้าง — ยืนยันว่า template "โมเดิร์นมินิมอล" และกลุ่มผู้รับ "Winai" โหลดกลับมาถูกต้องครบ ไม่ได้รับผลกระทบจากการแก้ไขนี้

## 3. Verification

- [x] 3.1 รัน `pnpm lint` (0 errors, warning เดิมที่ไม่เกี่ยวข้อง) และ `pnpm build` (สำเร็จ)
